<Window x:Class="BeautySalon.AdminWindows.RecordsManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeautySalon.AdminWindows"
        mc:Ignorable="d"
        FontFamily="Tahoma"
        Icon="/Resources/beauty_logo.ico"
        Title="Ближайшие записи" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#ff4a6d">
            <Button x:Name="BtnBack" Content="Назад" Background="#ff4a6d" Foreground="Aqua" Margin="10" HorizontalAlignment="Left" Click="BtnBack_Click"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10" HorizontalAlignment="Center">
                <Image Source="/Resources/beauty_logo.png"/>
                <TextBlock Text="Ближайшие записи" Foreground="White" FontSize="18" FontWeight="Bold" 
   VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <ComboBox x:Name="DateFilterComboBox" Width="120" SelectedIndex="0" SelectionChanged="DateFilterComboBox_SelectionChanged">
                <ComboBoxItem Content="Сегодня"/>
                <ComboBoxItem Content="Завтра"/>
                <ComboBoxItem Content="Все ближайшие"/>
            </ComboBox>
            <Button Content="Обновить" Background="#ffe4e1" Margin="10,0,0,0" Click="RefreshBtn_Click"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="10">
            <ItemsControl x:Name="RecordsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,5" Padding="10"
                                Background="White" CornerRadius="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding ServiceTitle}" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding ClientFullName}" Margin="0,0,0,3"/>
                                    <TextBlock Text="{Binding ClientEmail}" Margin="0,0,0,3"/>
                                    <TextBlock Text="{Binding ClientPhone}" Margin="0,0,0,3"/>
                                    <TextBlock Text="{Binding DateTimeString}" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding TimeRemaining}" FontSize="14" FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="Green"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsLessThanHour}" Value="True">
                                                        <Setter Property="Foreground" Value="Red"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock x:Name="LastUpdateText" FontStyle="Italic"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="RecordsCountText"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
