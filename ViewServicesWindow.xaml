<Window x:Class="BeautySalon.ViewServicesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeautySalon"
        mc:Ignorable="d"
        FontFamily="Tahoma"
        Icon="/Resources/beauty_logo.ico"
        Title="Список услуг" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#ff4a6d">
            <Button x:Name="BtnBack" Content="Назад" Background="#ff4a6d" Foreground="Aqua" Margin="10" HorizontalAlignment="Left" Click="BtnBack_Click"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10" HorizontalAlignment="Center">
                <Image Source="/Resources/beauty_logo.png"/>
                <TextBlock Text="Список услуг" Foreground="White" FontSize="18" FontWeight="Bold" 
           VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="10,5" Background="#ffe4e1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Поиск:" Margin="5" VerticalAlignment="Center"/>
                <TextBox x:Name="SearchTextBox" Width="150" Margin="5" Padding="5"
                  TextChanged="SearchTextBox_TextChanged"
                 ToolTip="Введите название услуги для поиска"/>

                <TextBlock Text="Сортировка:" Margin="5" VerticalAlignment="Center"/>
                <ComboBox x:Name="SortComboBox" Width="120" Margin="5" SelectedIndex="0" SelectionChanged="SortComboBox_SelectionChanged">
                    <ComboBoxItem Content="По умолчанию"/>
                    <ComboBoxItem Content="По цене (возр.)"/>
                    <ComboBoxItem Content="По цене (убыв.)"/>
                </ComboBox>

                <TextBlock Text="Фильтр по скидке:" Margin="10,5,5,5" VerticalAlignment="Center"/>
                <ComboBox x:Name="FilterComboBox"  Width="120" Margin="5" SelectedIndex="0" SelectionChanged="FilterComboBox_SelectionChanged">
                    <ComboBoxItem Content="Все услуги"/>
                    <ComboBoxItem Content="0-5%"/>
                    <ComboBoxItem Content="5-15%"/>
                    <ComboBoxItem Content="15-30%"/>
                    <ComboBoxItem Content="30-70%"/>
                    <ComboBoxItem Content="70-100%"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ListBox x:Name="ServicesListBox" Margin="10" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" Margin="0,5" Padding="10"
                               >
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Width="100" Height="100" Margin="0,0,10,0"
                                       Source="{Binding MainImagePath}" Stretch="UniformToFill"/>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" FontSize="16" 
                                               FontWeight="Bold" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" 
                                               Margin="0,0,0,5" MaxHeight="40" TextTrimming="CharacterEllipsis"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <TextBlock Text="Длительность: " FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding DurationInSeconds}"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Cost, StringFormat={}{0:C}}" 
                                               FontSize="14" HorizontalAlignment="Right"
                                               />
                                    <TextBlock Text="{Binding DiscountedPrice, StringFormat={}{0:C}}" 
                                               FontSize="16" FontWeight="Bold" Foreground="Red"
                                               HorizontalAlignment="Right"
                                               />
                                    <TextBlock Text="{Binding Discount, StringFormat={}Скидка {0:P0}}" 
                                               FontSize="12" Foreground="Green" FontWeight="Bold"
                                               HorizontalAlignment="Right"
                                               />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</Window>
