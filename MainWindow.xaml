<Window x:Class="BeautySalon.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeautySalon"
        mc:Ignorable="d"
        FontFamily="Tahoma"
        Icon="/Resources/beauty_logo.ico"
        Title="Авторизация" Height="450" Width="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#ff4a6d">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10" HorizontalAlignment="Center">
                    <Image Source="/Resources/beauty_logo.png"/>
                    <TextBlock Text="Авторизация для администраторов" Foreground="White" FontSize="18" FontWeight="Bold" 
                       VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <Button x:Name="BtnGuest" Content="Войти как гость" Background="#ff4a6d" Foreground="Aqua" Margin="10" HorizontalAlignment="Right" Click="BtnGuest_Click"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Background="White">
                <StackPanel Margin="20">
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Введите пароль для входа на админ-панель" Margin="0,5" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль:" Margin="0,5" VerticalAlignment="Center"/>
                    <TextBox Background="#ffe4e1" Grid.Row="1" Grid.Column="1" x:Name="PasswordTextBox" MaxLength="100" Margin="5" VerticalAlignment="Center"/>

                    <Button x:Name="BtnLogin" Content="Войти" Background="#ff4a6d" Foreground="White" Margin="10" Click="BtnLogin_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
